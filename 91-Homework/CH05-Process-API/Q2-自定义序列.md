# Q2 · 自定义序列（含嵌套退出）

## 题目

```sh
cd ~/ostep-homework/cpu-api
./fork.py -A a+b,b+c,c+d,c-,a+e,e-,a+f
```

动作序列：

```text
初始: a

1. a forks b
2. b forks c
3. c forks d
4. c EXITS        ← 注意：c 此时有子进程 d
5. a forks e
6. e EXITS
7. a forks f
```

---

## Step 1 · Prediction

| # | 动作 | 我预测的树 |
|---|---|---|
| 1 | `a forks b` | |
| 2 | `b forks c` | |
| 3 | `c forks d` | |
| 4 | `c EXITS` | |
| 5 | `a forks e` | |
| 6 | `e EXITS` | |
| 7 | `a forks f` | |

**重点想清楚第 4 步**：`c` 退出时，它的子进程 `d` 会去哪？

我的判断：

-

**最终进程树（手写）：**

```text

```

---

## Step 2 · Result

```sh
./fork.py -A a+b,b+c,c+d,c-,a+e,e-,a+f -c
```

```text

```

**第 4 步的实际结果是什么？和预测一致吗？**

-

---

## Step 3 · Explanation

**默认模式下，`c` 退出后 `d` 挂到了哪里？**

-

**换成 `-R` 呢？挂载位置有什么不同？**

```sh
./fork.py -A a+b,b+c,c+d,c-,a+e,e-,a+f -c -R
```

-

**换成 `-L` 呢？为什么第 4 步会失败？**

```sh
./fork.py -A a+b,b+c,c+d,c-,a+e,e-,a+f -c -L
```

-

---

## 反推练习（-t）

跑下面的命令，根据打印出的树猜出每一步的动作，**猜完再**加 `-c` 验证：

```sh
./fork.py -s 3 -t
```

| # | 我猜的动作 |
|---|---|
| 1 | |
| 2 | |
| 3 | |
| 4 | |
| 5 | |

验证结果：

```text

```

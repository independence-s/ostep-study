# Q1 · 随机进程树（seed 20）

## 题目

```sh
cd ~/ostep-homework/cpu-api
./fork.py -s 20
```

得到的动作序列：

```text
初始: a

1. a forks b
2. b EXITS
3. a forks c
4. c forks d
5. a forks e
```

---

## Step 1 · Prediction

先**别**运行 `-c`。在下表里手写每一步的树。

| # | 动作 | 我预测的树 |
|---|---|---|
| 1 | `a forks b` | |
| 2 | `b EXITS` | |
| 3 | `a forks c` | |
| 4 | `c forks d` | |
| 5 | `a forks e` | |

**最终进程树（手写）：**

```text

```

---

## Step 2 · Result

```sh
./fork.py -s 20 -c
```

在下面贴出实际输出：

```text

```

**哪一步预测错了？**（全对就写"全对"）

-

---

## Step 3 · Explanation

**为什么第 2 步 `b EXITS` 之后树会变成那个样子？**

-

**第 3 步新建的 `c`，为什么没有出现在 `b` 原来的位置上？**

-

---

## 延伸

- [ ] 加 `-R` 再跑一遍（`./fork.py -s 20 -c -R`），结果有变化吗？为什么？
- [ ] 加 `-L` 再跑一遍（`./fork.py -s 20 -c -L`），哪一步会失败？
- [ ] 用 `-F` 只给最终树（`./fork.py -s 20 -F`），练习一次推到底
